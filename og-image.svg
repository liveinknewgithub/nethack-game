<svg width="1200" height="630" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="bg" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1a1a2e"/>
      <stop offset="100%" style="stop-color:#0a0a1a"/>
    </linearGradient>
    <linearGradient id="titleGlow" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#ff6b6b"/>
      <stop offset="50%" style="stop-color:#ffe66d"/>
      <stop offset="100%" style="stop-color:#4ecdc4"/>
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="1200" height="630" fill="url(#bg)"/>

  <!-- Grid pattern (dungeon feel) -->
  <g opacity="0.1">
    <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
      <rect width="40" height="40" fill="none" stroke="#4a4a6a" stroke-width="1"/>
    </pattern>
    <rect width="1200" height="630" fill="url(#grid)"/>
  </g>

  <!-- Decorative dungeon tiles -->
  <text x="50" y="80" font-size="48" opacity="0.3">🧱🧱🧱🧱🧱🧱🧱🧱🧱🧱🧱🧱</text>
  <text x="50" y="580" font-size="48" opacity="0.3">🧱🧱🧱🧱🧱🧱🧱🧱🧱🧱🧱🧱</text>

  <!-- Main title -->
  <text x="600" y="200" font-family="Arial, sans-serif" font-size="100" font-weight="bold" fill="url(#titleGlow)" text-anchor="middle">
    🧙 EmojiHack 🐉
  </text>

  <!-- Subtitle -->
  <text x="600" y="280" font-family="Arial, sans-serif" font-size="36" fill="#a0a0c0" text-anchor="middle">
    A NetHack-Inspired Roguelike Adventure
  </text>

  <!-- Feature emojis row -->
  <text x="600" y="380" font-size="64" text-anchor="middle">
    ⚔️ 🛡️ 🧪 💰 🍖
  </text>

  <!-- Enemy parade -->
  <text x="600" y="480" font-size="56" text-anchor="middle">
    🐀 🐍 👺 💀 🐉
  </text>

  <!-- Call to action -->
  <rect x="400" y="520" width="400" height="60" rx="30" fill="#4ecdc4" opacity="0.9"/>
  <text x="600" y="560" font-family="Arial, sans-serif" font-size="28" font-weight="bold" fill="#0a0a1a" text-anchor="middle">
    Play Free in Browser
  </text>

  <!-- Border accent -->
  <rect x="0" y="0" width="1200" height="8" fill="url(#titleGlow)"/>
  <rect x="0" y="622" width="1200" height="8" fill="url(#titleGlow)"/>
</svg>
